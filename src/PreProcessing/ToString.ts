/**
 * Serializes a value into a string.
 * @param {any} value - The value to be processed
 * @return {number} - Resulting string
 */
export const TO_STRING = (value: any): string => {
	return `${value}`
}
